# Following travis outline from dfm/emcee3

sudo: false

language: python

python:
    - "3.5"

install:
    - pip install setuptools pytest pytest-cov coveralls emcee

script:
    - python setup.py test -a "--cov dessn -v"

after_success:
    - coveralls